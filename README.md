3.context provider for whole app
    - provider ( supabaseProvider ) to provide supabase data base whole app
        - wrap to layout.tsx
--> we can access to client supabase inside our application
    - use userhook to store subscription
        - types_db.ts --> defined type